FROM python:3.11-slim

WORKDIR /mlflow

RUN pip install --no-cache-dir "mlflow>=2.8.0,<4.0.0"

RUN mkdir -p /mlruns

EXPOSE 5000

CMD ["mlflow", "server", "--host", "0.0.0.0", "--port", "5000", "--backend-store-uri", "/mlruns"]
